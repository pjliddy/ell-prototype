{{log panels}}<div class=body-tabs><ul class="nav nav-tabs" role=tablist>{{#each panels as |panel|}} {{#if @first}}<li role=presentation class=active>{{!-- active tab --}} <a href=#{{panel.id}} aria-controls={{panel.id}} role=tab data-toggle=tab aria-expanded=true>{{else}}<li role=presentation>{{!-- non-active tab --}} <a href=#{{panel.id}} aria-controls={{panel.id}} role=tab data-toggle=tab>{{/if}} {{panel.title}}</a></li>{{/each}}</a></li></ul><div class=tab-content>{{#each panels as |panel|}}<div role=tabpanel class="tab-pane {{#if @first}} active{{/if}}" id={{panel.id}}>{{panel.title}}</div>{{/each}}</div></div>